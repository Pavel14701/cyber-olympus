window.onload=function(){const canvas=document.querySelector('.lightning');const ctx=canvas.getContext('2d');canvas.width=window.innerWidth;canvas.height=window.innerHeight;const points=[];let lastTime=0;document.addEventListener('mousemove',(event)=>{const x=event.clientX;const y=event.clientY;points.push({x,y});if (points.length>30){points.shift();}});window.addEventListener('resize',()=>{canvas.width=window.innerWidth;canvas.height=window.innerHeight;});const drawLightning=function(){ctx.strokeStyle='#FFFF00';ctx.lineWidth=5;ctx.beginPath();if(points.length>0){ctx.moveTo(points[0].x,points[0].y);for(let i=1;i<points.length;i++){const prevPoint=points[i-1];const currPoint=points[i];const midX=(prevPoint.x+currPoint.x)/2+(Math.random()*19-5);const midY=(prevPoint.y+currPoint.y)/2+(Math.random()*19-5);ctx.quadraticCurveTo(prevPoint.x,prevPoint.y,midX,midY);}ctx.lineTo(points[points.length-1].x,points[points.length-1].y);}ctx.stroke();};const animate=function(time){if (time-lastTime>30){ctx.clearRect(0,0,canvas.width,canvas.height);drawLightning();if (points.length>0){points.shift();}lastTime=time;}requestAnimationFrame(animate);};animate();};